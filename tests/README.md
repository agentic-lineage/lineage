# Tests

Go unit tests live beside the packages they cover. Higher-level fixtures and
integration tests live here; `receiver_flow_test.go` drives the checked-in
author-to-receiver package flow through the CLI.
